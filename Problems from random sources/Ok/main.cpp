#include <iostream>
#include <math.h>
using namespace std;

int main()
{
    int OK,i,n,sqrt;
    OK=1;//pp că nr prim
    for (i=2;i<=sqrt(n);i++)
        if(n%i==0)
            //o-false
            //i<n 1.000.000 operatii
            //i<=n/2 50000
}
