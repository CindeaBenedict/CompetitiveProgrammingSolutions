#include <bits/stdc++.h>

using namespace std;

int main()
{
    int s = 0;
    int tmp =-1;
    while (tmp != 0)
    {
    cin >> tmp;
    s+= tmp;
    }
    cout << s;
    return 0;
}